package com.br.hermescomercialnetbeans.controller;

import javax.swing.table.DefaultTableModel;

/**
 * Controller para gerenciar operações de venda
 * @author marcos
 */
public class VendaController {
    
    public VendaController() {
        // Construtor vazio
    }
    
    /**
     * Finaliza a compra
     * @param modelo Modelo da tabela de itens
     * @param total Total da venda
     */
    public void finalizarCompra(DefaultTableModel modelo, double total) {
        // Implementação básica para resolver erros de compilação
        System.out.println("Finalizando compra - Total: R$ " + total);
        // Lógica de finalização pode ser implementada posteriormente
    }
    
    // Métodos de controle de venda podem ser adicionados aqui
    // Por enquanto, classe criada para resolver erros de compilação
}
